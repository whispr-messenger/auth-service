# Application
NODE_ENV=development
PORT=3000

# Database PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=auth_service

# Redis Cache
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_PRIVATE_KEY=-----BEGIN EC PRIVATE KEY-----
# Générer avec: openssl ecparam -genkey -name prime256v1 -noout -out private-key.pem
-----END EC PRIVATE KEY-----

JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----
# Générer avec: openssl ec -in private-key.pem -pubout -out public-key.pem
-----END PUBLIC KEY-----

JWT_ACCESS_TOKEN_EXPIRY=1h
JWT_REFRESH_TOKEN_EXPIRY=7d

# SMS Configuration (à configurer selon le fournisseur)
SMS_PROVIDER=twilio
SMS_API_KEY=AC894b9c7a5e068d99f9d8f721db88bca
SMS_API_SECRET=4b93003a4af2db8795ac8769179952c6
SMS_FROM_NUMBER=+33712345678

# Rate Limiting
RATE_LIMIT_TTL=60000
RATE_LIMIT_MAX=10

# Security
BCRYPT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=900000

# Two Factor Authentication
TWO_FACTOR_ISSUER=AuthService
TWO_FACTOR_WINDOW=2

# Device Management
MAX_DEVICES_PER_USER=5
DEVICE_CHALLENGE_EXPIRY=300
