# Configuration CORS

## Variables

La liste des origines autorisées est dans `CORS_ORIGINS`.

```
CORS_ORIGINS=https://whispr.fr,https://app.whispr.fr
```

Le header `X-Device-Type` est autorisé pour identifier le type d'appareil.
